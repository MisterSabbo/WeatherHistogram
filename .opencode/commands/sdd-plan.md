---
description: SDD Phase 3 — Create implementation plan from a spec
agent: general
---

Run SDD Phase 3 (PLAN).

1. Read `.agents/skills/sdd-orchestrator/agent-plan.md` for the full process
2. Follow the Technical Planner role to produce:
   - `plans/<feature-name>/plan.md` — architecture decisions and implementation approach
   - `plans/<feature-name>/tasks.md` — ordered, actionable task breakdown
3. Check for `[NEEDS CLARIFICATION]` markers in the spec before proceeding
4. Output both file paths and the number of tasks created

Spec path: $ARGUMENTS
