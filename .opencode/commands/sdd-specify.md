---
description: SDD Phase 2 — Write specification for a module
agent: general
---

Run SDD Phase 2 (SPECIFY).

1. Read `.agents/skills/sdd-orchestrator/agent-specify.md` for the full process
2. Follow the Specification Author role to produce a spec at `specs/<path>.md`
3. Read the source code (if it exists), extract API, dependencies, state access, business logic, and edge cases
4. Write the spec following the template in the skill file
5. Mark any ambiguities with `[NEEDS CLARIFICATION: ...]`
6. Output the spec path and any clarification items

Module path: $ARGUMENTS
