# Repository Language

All repository files must be written in English, including code identifiers,
comments, commit messages, documentation, config files, and asset naming.

The only exceptions are files that are explicit translations for a specific
locale (e.g. `src/utils/i18n.js` entries, locale-specific JSON, or
translated documentation variants).
